.next
node_modules
pnpm-lock.yaml
.bkit

# 손으로 쓴 문서는 Prettier가 건드리지 않는다. 표 정렬·줄바꿈은 글쓴이 몫이다.
*.md

# scripts/build-korea-map.mjs가 생성한다. 손으로 고치지 않고, 포맷도 건드리지 않는다.
# (넣지 않으면 pnpm format이 실행될 때마다 2000줄짜리 무의미한 diff가 생긴다)
lib/geo/korea-sigungu.json
